#!/bin/bash

# Rechercher tous les processus master Gunicorn
for pid in $(ps aux | grep '[g]unicorn' | grep 'master' | awk '{print $2}'); do
  echo "Rechargement gracieux du processus Gunicorn avec PID: $pid"
  kill -HUP $pid
done

echo "Rechargement terminé."
